### Spec Compliance

- ✅ Spec compliant. `GemmMode` has exactly the three requested variants with Deterministic as the derived default and the exact strict environment spellings (`src/mamba_ssm/gpu/gemm_mode.rs:14`, `src/mamba_ssm/gpu/gemm_mode.rs:39`, `src/mamba_ssm/gpu/gemm_mode.rs:54`). It is re-exported through both required public paths (`src/mamba_ssm/gpu/context.rs:8`, `src/mamba_ssm/gpu/mod.rs:39`).
- ✅ `GpuCtx` now has one canonical `Cell<GemmMode>` and derives the legacy booleans and `GemmPolicy` fields from it while retaining family, tensor-core, f32, and half policy independently (`src/mamba_ssm/gpu/context.rs:521`, `src/mamba_ssm/gpu/context.rs:1169`, `src/mamba_ssm/gpu/context.rs:1683`). Fresh and explicit constructors use the requested Triad / tensor-core-on / ExactScalarFmaV1 / TiledParityV1 defaults and do not read ambient mode selectors (`src/mamba_ssm/gpu/context.rs:617`, `src/mamba_ssm/gpu/context.rs:706`, `src/mamba_ssm/gpu/context.rs:735`).
- ✅ Environment resolution is presence-sensitive and implements the canonical/legacy table, vendor-mode rejection of deterministic controls, retained `fixed` alias, arch-rung validation, tiled-half default, and stream-K/tensor-core validation before construction (`src/mamba_ssm/gpu/context.rs:37`, `src/mamba_ssm/gpu/context.rs:71`, `src/mamba_ssm/gpu/context.rs:116`, `src/mamba_ssm/gpu/context.rs:239`, `src/mamba_ssm/gpu/context.rs:380`).
- ✅ Mode transitions are fallible and failure-atomic: complete custom policy plus route-recorder and live capture state are checked before handle mutation; the old handle math is queried; the mode cell is published only after success; failed updates are verified/restored; unverified rollback records a stable unusable-context diagnostic (`src/mamba_ssm/gpu/context.rs:1086`, `src/mamba_ssm/gpu/context.rs:1147`, `src/mamba_ssm/gpu/gemm_mode.rs:118`). Same-mode remains a no-op on a usable context (`src/mamba_ssm/gpu/context.rs:1088`).
- ✅ Deterministic and Pedantic select `CUBLAS_PEDANTIC_MATH`, Fast selects `CUBLAS_TF32_TENSOR_OP_MATH`, and context-aware GemmEx maps all three dtypes to 32F or 32F_PEDANTIC while Deterministic fails closed at the vendor boundary (`src/mamba_ssm/gpu/gemm_mode.rs:78`, `src/mamba_ssm/gpu/gemm_mode.rs:85`, `src/mamba_ssm/gpu/blas.rs:29`). Dtype-only no-context helpers retain their prior compute selection (`src/mamba_ssm/gpu/blas.rs:3092`, `src/mamba_ssm/gpu/blas.rs:3223`).
- ✅ Deprecated setters retain their signatures and implement the complete requested transition table through the fallible canonical setter, panicking only because their old signatures cannot return errors (`src/mamba_ssm/gpu/context.rs:1176`, `src/mamba_ssm/gpu/context.rs:1215`, `src/mamba_ssm/gpu/context.rs:1760`, `src/mamba_ssm/gpu/gemm_mode.rs:95`).
- ✅ Vendor policy identity is versioned without relabelling V1: policy revision 6, explicit vendor-policy revision 2 in dispatch-policy.v5, and CUBLAS_POLICY_V2 at the next bit is used by active vendor routes (`src/mamba_ssm/gpu/kernel_identity.rs:50`, `src/mamba_ssm/gpu/kernel_identity.rs:2780`, `src/mamba_ssm/gpu/kernel_identity.rs:2834`, `src/mamba_ssm/gpu/kernel_identity.rs:2848`). The prepared diff contains no CUDA/header body, custom selector/admission, compiler identity, or architecture-gate changes.
- ✅ The public enum, variants, constructors, setter/getter, compatibility adapters, environment behavior, capture restrictions, failure behavior, and custom-versus-vendor TF32 distinction have plain technical Rustdoc and compiling examples (`src/mamba_ssm/gpu/gemm_mode.rs:3`, `src/mamba_ssm/gpu/context.rs:601`, `src/mamba_ssm/gpu/context.rs:632`, `src/mamba_ssm/gpu/context.rs:663`, `src/mamba_ssm/gpu/context.rs:1060`).
- ⚠️ Cannot verify from this task diff: model-wide no-cuBLAS closure, high-level M1/M3 mode construction, direct tied-head/M3 projection routing, benchmark migration, and RTX 5090 qualification are explicitly staged dependencies, not Task 901 requirements. No in-scope Task 901 requirement remains unverifiable from the diff plus the supplied raw verification packets.

### Strengths

- The mode table, adapter table, and foreign-handle transaction are isolated in a small pure module, making the policy independently testable without weakening the production CUDA path (`src/mamba_ssm/gpu/gemm_mode.rs:39`, `src/mamba_ssm/gpu/gemm_mode.rs:118`, `src/mamba_ssm/gpu/gemm_mode.rs:171`).
- The failure path is unusually careful: it distinguishes recoverable foreign-call errors from an unverified rollback and prevents later supported GEMM/capture/replay work through one diagnostic (`src/mamba_ssm/gpu/context.rs:1123`, `src/mamba_ssm/gpu/context.rs:1147`).
- Qualification leases preserve dormant policy and were repaired tests-first so a failed mode setup is attempted once, restores every custom field, returns the original error, and releases the active token (`src/mamba_ssm/gpu/gemm_bi_triad/qualification.rs:331`, `src/mamba_ssm/gpu/gemm_bi_triad/qualification.rs:5185`, `src/mamba_ssm/gpu/gemm_bi_triad/qualification.rs:5874`).
- Tests cover strict public names/default, all nine live transitions, adapter ordering, real handle math, capture/recording rejection, all dtype compute mappings, environment conflicts/defaults, transaction failures, identity drift, and policy preservation (`tests/gemm_mode_api.rs:10`, `tests/gemm_mode_api.rs:81`, `tests/gemm_mode_api.rs:107`, `tests/gemm_mode_live.rs:8`, `src/mamba_ssm/gpu/gemm_mode.rs:203`, `tests/kernel_identity.rs:609`).
- Supplied raw evidence is coherent: the missing-API RED fails with the expected 14 diagnostics (`internal/perf/gemm-mode-api-20260910/red/build.log:2`); the regression RED observes two transition attempts instead of one (`internal/perf/gemm-mode-api-20260910/lease-red/test.log:4`); the unchanged regression passes after repair (`internal/perf/gemm-mode-api-20260910/lease-green/test.log:2`); final API, live, unit, environment, compute, qualification, identity, Rustdoc, doctest, all-target, and non-CUDA checks all pass (`internal/perf/gemm-mode-api-20260910/green-fix1/api-tests.log:2`, `internal/perf/gemm-mode-api-20260910/green-fix1/live-tests.log:2`, `internal/perf/gemm-mode-api-20260910/green-fix1/mode-unit-tests.log:2`, `internal/perf/gemm-mode-api-20260910/green-fix1/all-targets-check.log:3371`).
- Focused outside-diff risk check: the central unusable-context guard reaches ordinary graph capture, physical capture preparation/binding/replay, and standard captured-plan replay through `begin_gemm_route_recording`, `PhysicalReplayProvenance::validate`, and `CapturedGemmGraphPlan::with_validated_launch` (`src/mamba_ssm/gpu/context.rs:1258`, `src/mamba_ssm/gpu/graph_capture.rs:106`, `src/mamba_ssm/gpu/kernel_identity.rs:4373`, `src/mamba_ssm/gpu/kernel_identity.rs:4777`). No bypass was found among the task's supported context-aware boundaries.

### Issues

#### Critical (Must Fix)

- None.

#### Important (Should Fix)

- None.

#### Minor (Nice to Have)

- `src/mamba_ssm/gpu/blas.rs:3448` — the retained dispatcher comment still says `M=1` selects matvec, `M>=2` selects cuBLAS, and the custom WMMA kernels are not in the default path. That contradicts both the new Deterministic default and the mode-dependent branches immediately below (`src/mamba_ssm/gpu/blas.rs:3462`, `src/mamba_ssm/gpu/blas.rs:3490`). Rewrite the overview around Deterministic versus vendor modes so future routing work is not guided by the obsolete cuBLAS-default description.
- `internal/perf/gemm-mode-api-20260910/green-fix1/all-targets-check.log:2` — final all-target compilation passes but emits hundreds of deprecation warnings from benchmark/test callers after the adapters became deprecated; Rustdoc also retains the separately documented pre-existing private-link warning (`internal/perf/gemm-mode-api-20260910/green-fix1/rustdoc.log:2`). Benchmark/test migration is explicitly deferred, so this does not block Task 901, but the follow-on should replace each call with its intended `CublasFast`, `CublasPedantic`, or Deterministic baseline rather than suppress the warnings.

### Assessment

**Task quality:** Approved

**Reasoning:** The implementation matches the canonical mode, transition, environment, identity, and guard contracts, with strong tests-first and live CUDA evidence. The two remaining items are non-behavioral cleanup: one stale internal routing comment and warning debt already assigned to the staged benchmark/test migration.
