# Task7 source-gate fix round 1

Continue the same task, branch, implementation ownership and sole Ada lease.
No source kernel, loader, selector, numeric/schedule/tuning revision changes.
The binding Task7 brief and incumbent ruling remain authoritative.

Read the complete source review:
`ada-f32-tf32-toolkit-task7-source-review.md`, SHA-256
`0bcb28ac57cefc7f5a5b10a0cf8050de4768e283f3bd1980626bb907f7cfa43f`.
Root checked and accepts all three Important findings. Critical/Minor none.

Fix exactly these contract gaps with focused RED/GREEN evidence:

1. Before case allocation or workflow launches, reject actual compiler
   numeric ABI other than5 and schedule other than8. Emit actual checked
   values into JSONL identity, validate them independently in the analyzer,
   and test each wrong/missing revision separately. Keep actual tuning43.
2. Compare live Fixed compiled artifact against confirm's validated screen
   artifact before `single_term_controls` or any case/workflow kernel launch.
   Add a negative that proves mismatch rejects before the first launch;
   merely asserting an eventual failure is insufficient.
3. Compare saved complete A/B/bias bytes and all input/output guards directly
   after timed observations and before extra eager/graph/poison workflows.
   Keep the later check too. Add sequencing negatives for A/B/bias mutations
   introduced at the timing boundary, including a later replay that would
   restore the original value, so the earlier check must catch the corruption.

Keep the implementation narrow; do not redesign the harness or modify
unrelated code. Root preserved byte-identical final4 copies of support and
the four Python tools under `task7-final4-freeze/` for fix-only review.
All final4 functional runs remain history; no screen21/confirm101 occurred.

After focused RED/GREEN and updated Python regression, freeze source/tools,
rebuild the same two toolkit test binaries in distinct final5 paths with the
same environment constructor, run matching host/nonignored checks, and send
the changed-file hashes and fix report. Final5 functional smoke1 may run on
all four literal families while root performs fix-only re-review; preserve
all final4 files and do not overwrite an attempt. Screen21 remains blocked
until root explicitly accepts the re-review and final5 functional closure.

Write a new `ada-f32-tf32-toolkit-task7-fix1-report.md` containing each finding,
change and RED/GREEN test evidence plus source/binary/command identities.
Update the cumulative report with a clearly separated current final5 section;
do not relabel the prior final4 results. Mirror logs without rerunning them.
Root owns staging, commits, handoff and ledger. No new branch or deletion.
